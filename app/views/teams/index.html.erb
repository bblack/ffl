<h1>Teams</h1>
<% if @current_league %>
  <table>
    <tr>
      <th>Team name</th>
      <th>Payroll</th>
      <% @current_league.positions.each do |p| %>
        <th><%= p %>s</th>
      <% end %>
    </tr>
    <% teams = @current_league.teams %>
    <% teams.each do |team| %>
      <tr>
        <td><a href="/teams/<%= team.id %>"><%= team.name %></a></td>
        <td><%= team.payroll %></td>
      </tr>
    <% end %>
  </table>
<% end %>